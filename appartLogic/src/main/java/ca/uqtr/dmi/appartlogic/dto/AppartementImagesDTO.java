package ca.uqtr.dmi.appartlogic.dto;
/*
import lombok.AllArgsConstructor;
import org.springframework.core.io.Resource;
import lombok.Data;
@Data
@AllArgsConstructor*/
public class AppartementImagesDTO {
    /*private Long id;
    private Long appartementId;
    private String image;//Path de l'image
    private Resource imageRessource; // Champ pour l'image à envoyer au client


    public static AppartementImagesDTO from(AppartementImages appartementImages) {
        return new AppartementImagesDTO(
                appartementImages.getId(),
                appartementImages.getAppartementId(),
                appartementImages.getImage()
        );
    }

    public static AppartementImages toEntity(AppartementImagesDTO appartementImagesDTO) {
        AppartementImages appartementImages = new AppartementImages();
        appartementImages.setId(appartementImagesDTO.getId());
        appartementImages.setAppartementId(appartementImagesDTO.getAppartementId());
        appartementImages.setImage(appartementImagesDTO.getImage());
        return appartementImages;
    }*/

}
